{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)docs/**' ':(exclude)AGENTS.md' ':(exclude).gitignore'",
  "regions": ["sin1"],
  "crons": [
    {
      "path": "/api/cron/trades",
      "schedule": "0 18 * * 0"
    }
  ]
}
